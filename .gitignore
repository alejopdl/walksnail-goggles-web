# Local-only binaries and captures — do not commit
_local_refs/
captures/
*.apk
*.dex

# Local-only notes & dev tooling — not published
tools/
poc/python/tests/test_integration_live.py
poc/python/tests/test_webapp_verify.py

# Internal process docs (status log, decision log, user feedback).
# THIS REPO IS PUBLIC — working-process notes never get committed here.
docs/internal/

# Python PoC
poc/python/.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/

# OS / editor
.DS_Store
*.swp

# AI tooling (local only)
.agents/
.claude/
.gemini/

# Flutter mobile app lives in a separate location, not in this public repo
walksnail_mobile/
